services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai_typer_server_prod
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
